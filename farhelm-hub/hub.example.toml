[hub]
bind = "127.0.0.1:8787"
database = "/var/lib/farhelm/farhelm.db"

[admin]
user = "admin"
password_hash = "$argon2id$generated-by-farhelm-hub-install"

[agents]
# Credentials are enrolled by one-time Console pairing codes and stored hashed in SQLite.
